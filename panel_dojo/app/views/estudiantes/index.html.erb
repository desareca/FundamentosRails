<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><% title @dojo.sucursal %></title>
</head>
<body>
    <div class="container">
        <div class="row mt-5 mb-3">
            <h1 class="d-inline-block ml-0 mr-2"><%= @dojo.sucursal %></h1>
            <%= link_to "Edit","/dojos/" + @dojo.id.to_s + "/edit" , class:"d-inline-block align-bottom"%>
        </div>
        <div class="row mb-3">
            <div class="col-3 py-4">
                <p>Street: <%= @dojo.calle %></p>
                <p>City: <%= @dojo.ciudad %></p>
                <p>State: <%= @dojo.estado %></p>
            </div>
        </div>
        <tr class="col-3">
            <td><%= link_to "Home ", "/dojos/"%> | <%= link_to " Add Student","/dojos/" + @dojo.id.to_s + "/estudiantes/new" %></td>
        </tr>
    </div>
</body>
</html>

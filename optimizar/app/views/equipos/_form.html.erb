<%= form_with(model: equipo, local: true) do |form| %>
  <% if equipo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equipo.errors.count, "error") %> prohibited this equipo from being saved:</h2>

      <ul>
        <% equipo.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :mascota %>
    <%= form.text_field :mascota %>
  </div>

  <div class="field">
    <%= form.label :estadio %>
    <%= form.text_field :estadio %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
